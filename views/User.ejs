<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All users info</title>   
    
    <style>
        table, th, td, tr{
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <%let count=1%>

    <form method="get" action="user/newUser">
        <button>Add new User</button>
    </form>
<br>
    <form action="/" method="get"><button>Total No of User</button></form>

    <table >
        <tr>
            <th>Serial No.</th>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
        </tr>
        <%for(user of users){%>
        <tr>
            <td><%=count%></td>
            <td><%=user.id%></td>
            <td><%=user.username%></td>
            <td><%=user.email%></td>
            <td>
                <form method="get" action="/user/<%=user.id%>/edit">
                    <button>Edit usrename</button>
                </form>
            </td>
            <td>
                <form method="post" action="/user/<%=user.id%>?_method=DELETE"><button>Delete</button></form>
            </td>
            <%count++;%>
        </tr>
        <%}%>
    </table>
    
</body>
</html>